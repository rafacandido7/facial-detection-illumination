---
phase: 02-datasets-annotations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/convert_labelme.py
  - dataset/proprio/gt.json
autonomous: false
requirements:
  - OWN-01
  - OWN-02

must_haves:
  truths:
    - "dataset/proprio/{bright,dark,lateral,overexposed}/ directories exist"
    - "~5 images per condition (~20 total) are present after human capture"
    - "Each image has a labelme JSON annotation with at least one rectangle shape"
    - "convert_labelme.py produces dataset/proprio/gt.json with all annotated images"
    - "gt.json file keys are PROJECT_ROOT-relative (no leading slash or ./)"
    - "gt.json dataset field is proprio and iou_threshold is 0.5"
  artifacts:
    - path: "scripts/convert_labelme.py"
      provides: "labelme JSON to gt.json conversion script"
    - path: "dataset/proprio/gt.json"
      provides: "GT JSON for proprio dataset"
      contains: "proprio"
    - path: "dataset/proprio/bright/"
      provides: "~5 captured bright-condition images"
    - path: "dataset/proprio/dark/"
      provides: "~5 captured dark-condition images"
    - path: "dataset/proprio/lateral/"
      provides: "~5 captured lateral-condition images"
    - path: "dataset/proprio/overexposed/"
      provides: "~5 captured overexposed-condition images"
  key_links:
    - from: "labelme *.json files"
      to: "convert_labelme.py parse_labelme_json()"
      via: "shapes[].points [[x1,y1],[x2,y2]] where shape_type=rectangle"
      pattern: "shape_type.*rectangle"
    - from: "gt.json images[].file"
      to: "evaluate.py line 100"
      via: "str(img_file.resolve().relative_to(PROJECT_ROOT))"
      pattern: "relative_to.PROJECT_ROOT."

user_setup:
  - service: labelme
    why: "Manual bounding box annotation of own dataset images (D-08)"
    install: "pip install labelme"
    usage: "labelme --nodata  (the --nodata flag prevents base64 image embedding)"
---

<objective>
Create the directory structure for dataset/proprio/, write scripts/convert_labelme.py,
pause for human image capture and labelme annotation, then run the conversion to produce
dataset/proprio/gt.json.

This plan is autonomous=false because OWN-01 requires physical image capture by the team
(D-07: human prerequisite). The executor creates the structure and conversion script first,
then waits at a checkpoint for the human to capture images and annotate them with labelme,
then resumes to run the conversion.

Output: scripts/convert_labelme.py, dataset/proprio/ folder structure, dataset/proprio/gt.json.
</objective>

<execution_context>
@/Users/candido/.claude/get-shit-done/workflows/execute-plan.md
@/Users/candido/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-datasets-annotations/02-RESEARCH.md
@.planning/phases/02-datasets-annotations/02-CONTEXT.md
@src/evaluate.py

<interfaces>
evaluate.py GT key contract (lines 100-101):
  rel_path = str(img_path.resolve().relative_to(PROJECT_ROOT))
  gt_entry = gt.get(rel_path) if gt else None

gt.json "file" values must be PROJECT_ROOT-relative, e.g.:
  "dataset/proprio/bright/img001.jpg"
  (no leading slash, no "./" prefix)

GT JSON structure:
  {"dataset": "proprio", "iou_threshold": 0.5, "images": [
    {"file": "dataset/proprio/bright/img001.jpg", "condition": "bright",
     "faces": [{"x1": 120, "y1": 80, "x2": 280, "y2": 300}]}
  ]}

labelme JSON structure (per-image file, shape_type=rectangle):
  {
    "imagePath": "img001.jpg",          # filename only, relative to JSON file's directory
    "shapes": [
      {
        "shape_type": "rectangle",
        "points": [[x1, y1], [x2, y2]] # corner order may vary — use min/max
      }
    ]
  }

condition = json_path.parent.name  (bright / dark / lateral / overexposed)
image file = (json_path.parent / data["imagePath"]).resolve()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create folder structure and write scripts/convert_labelme.py</name>
  <files>scripts/convert_labelme.py</files>

  <read_first>
    - .planning/phases/02-datasets-annotations/02-RESEARCH.md — "Complete convert_labelme.py
      skeleton" section (authoritative base), Pattern 4 (labelme JSON parsing), Pitfall 4
      (imagePath is relative), Pitfall 5 (dataset_name in results dir), Anti-Patterns section
    - src/evaluate.py lines 28-38 and 100-101 — confirm GT key format contract
  </read_first>

  <action>
Step A — Create dataset/proprio/ directory structure:
  mkdir -p dataset/proprio/bright
  mkdir -p dataset/proprio/dark
  mkdir -p dataset/proprio/lateral
  mkdir -p dataset/proprio/overexposed

  Also create placeholder README so git tracks the dirs:
  Write "# Capture ~5 images per condition here before running convert_labelme.py" to
  dataset/proprio/bright/README.txt, and same for dark/, lateral/, overexposed/.

Step B — Create scripts/convert_labelme.py:
  Use RESEARCH.md "Complete convert_labelme.py skeleton" as the authoritative base.
  Verify these requirements are satisfied:

  MODULE DOCSTRING at top:
    """
    Convert labelme JSON annotations to unified gt.json for dataset/proprio/.

    Usage:
      1. Capture ~5 images per condition into dataset/proprio/{bright,dark,lateral,overexposed}/
      2. Annotate each image with: labelme --nodata
         Draw one rectangle per face; save; labelme writes a .json beside the image.
      3. Run: python scripts/convert_labelme.py
      4. Verify: dataset/proprio/gt.json created with one entry per annotated image.
    """

  CONSTANTS (exact):
    PROJECT_ROOT = Path(__file__).resolve().parent.parent
    PROPRIO_DIR = PROJECT_ROOT / "dataset/proprio"

  parse_labelme_json(json_path: Path) -> dict | None:
    - Open json_path and json.load()
    - For each shape in data.get("shapes", []):
        if shape.get("shape_type") != "rectangle": continue
        pts = shape["points"]  # [[x1,y1],[x2,y2]], corner order may vary
        faces.append({
          "x1": int(min(pts[0][0], pts[1][0])),
          "y1": int(min(pts[0][1], pts[1][1])),
          "x2": int(max(pts[0][0], pts[1][0])),
          "y2": int(max(pts[0][1], pts[1][1])),
        })
    - If no faces found: print(f"WARNING: no rectangle annotation in {json_path}"); return None
    - img_file = (json_path.parent / data["imagePath"]).resolve()
      (CRITICAL: join with json_path.parent — imagePath is filename-only, not full path)
    - rel = str(img_file.relative_to(PROJECT_ROOT))
    - condition = json_path.parent.name  (bright / dark / lateral / overexposed)
    - return {"file": rel, "condition": condition, "faces": faces}

  main():
    - jsons = sorted(PROPRIO_DIR.rglob("*.json"))
      Filter out gt.json itself: jsons = [j for j in jsons if j.name != "gt.json"]
    - entries = [e for j in jsons if (e := parse_labelme_json(j)) is not None]
    - If len(entries) == 0: print("WARNING: no annotations found — run labelme first"); sys.exit(1)
    - gt = {"dataset": "proprio", "iou_threshold": 0.5, "images": entries}
    - out = PROPRIO_DIR / "gt.json"
    - json.dump(gt, open(out, "w"), indent=2)
    - print(f"GT written: {out} ({len(entries)} images)")
    - Self-check: print sample file key; assert not startswith('/') and not startswith('./')

  IMPORTS: import json, sys; from pathlib import Path
  ENTRYPOINT: if __name__ == "__main__": main()
  </action>

  <acceptance_criteria>
    - dataset/proprio/bright/, dark/, lateral/, overexposed/ directories exist
    - scripts/convert_labelme.py exists and parses without syntax error
    - grep "shape_type.*rectangle\|rectangle.*shape_type" scripts/convert_labelme.py matches
    - grep "imagePath" scripts/convert_labelme.py matches (labelme field)
    - grep "json_path.parent" scripts/convert_labelme.py matches (correct path join)
    - grep "relative_to(PROJECT_ROOT)" scripts/convert_labelme.py matches
    - grep "WARNING" scripts/convert_labelme.py matches (no-annotation warning path)
    - grep "gt.json" scripts/convert_labelme.py matches (filters out gt.json from rglob)
    - grep "proprio" scripts/convert_labelme.py matches (dataset name)
    - python -c "import ast; ast.parse(open('scripts/convert_labelme.py').read()); print('OK')" exits 0
  </acceptance_criteria>

  <verify>
    <automated>python -c "
import ast
src = open('scripts/convert_labelme.py').read()
ast.parse(src)
assert 'shape_type' in src, 'Must check shape_type == rectangle'
assert 'imagePath' in src, 'Must use labelme imagePath field'
assert 'json_path.parent' in src, 'Must join imagePath with json_path.parent'
assert 'relative_to(PROJECT_ROOT)' in src, 'Must use relative_to(PROJECT_ROOT) for GT keys'
assert 'WARNING' in src, 'Must warn on missing annotation'
assert 'gt.json' in src, 'Must exclude gt.json from rglob results'
assert 'proprio' in src, 'dataset name must be proprio'
print('convert_labelme.py static checks passed')
"
ls dataset/proprio/bright/ dataset/proprio/dark/ dataset/proprio/lateral/ dataset/proprio/overexposed/ 2>&1 | head -8</automated>
  </verify>

  <done>
    - scripts/convert_labelme.py exists with correct syntax and all static checks pass
    - dataset/proprio/{bright,dark,lateral,overexposed}/ directories exist
    - Script logic: filters gt.json from rglob, uses min/max on points, joins imagePath with
      json_path.parent, builds PROJECT_ROOT-relative file keys, writes dataset=proprio gt.json
  </done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Human captures images and annotates with labelme</name>

  <what-built>
    Task 1 created dataset/proprio/{bright,dark,lateral,overexposed}/ and
    scripts/convert_labelme.py. The conversion script is ready to run once images exist.
  </what-built>

  <how-to-verify>
    Complete these steps before resuming:

    1. Install labelme if not already installed:
         pip install labelme

    2. Capture ~5 images per condition (~20 total) with your camera or phone.
       Lighting conditions to capture:
         bright      — face well-lit, soft or outdoor light, no harsh shadows
         dark        — low ambient light, face partially lit or dim
         lateral     — strong directional light from one side (lamp or window at 90 degrees)
         overexposed — direct flash or bright backlight causing blown-out highlights

    3. Copy images into their condition folders:
         dataset/proprio/bright/     (e.g. img001.jpg, img002.jpg ...)
         dataset/proprio/dark/
         dataset/proprio/lateral/
         dataset/proprio/overexposed/

    4. Annotate each image with labelme (--nodata prevents base64 embedding):
         labelme --nodata dataset/proprio/bright/img001.jpg
         (or open the whole folder: labelme --nodata dataset/proprio/bright/)
       For each image:
         - Select "Create Rectangle" tool
         - Draw a rectangle tightly around the face
         - Label it "face" (or any label — the script only reads shape_type=rectangle)
         - File > Save  (creates img001.json beside the image)

    5. Verify each image folder has matching .json files:
         ls dataset/proprio/bright/    # should show both .jpg and .json files

    After completing all annotations, type "done" to resume.
  </how-to-verify>

  <resume-signal>Type "done" after all images are captured and annotated with labelme</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Run convert_labelme.py to produce dataset/proprio/gt.json</name>
  <files>dataset/proprio/gt.json</files>

  <read_first>
    - scripts/convert_labelme.py — read the full file to understand what it expects before running
    - dataset/proprio/ — confirm images and .json annotation files are present
  </read_first>

  <action>
Run the conversion script from project root:
  python scripts/convert_labelme.py

If the script exits with "WARNING: no annotations found", the human checkpoint was not
completed — stop and instruct the user to finish labelme annotation first.

After successful run, verify gt.json by running:
  python -c "
import json
from pathlib import Path
d = json.load(open('dataset/proprio/gt.json'))
assert d['dataset'] == 'proprio'
assert d['iou_threshold'] == 0.5
assert len(d['images']) >= 1
for entry in d['images']:
    assert not entry['file'].startswith('/')
    assert not entry['file'].startswith('./')
    assert entry['file'].startswith('dataset/proprio/')
    assert entry['condition'] in ('bright','dark','lateral','overexposed')
    assert len(entry['faces']) >= 1
print(f'gt.json valid: {len(d[\"images\"])} entries across conditions')
from collections import Counter
counts = Counter(e['condition'] for e in d['images'])
print('Condition counts:', dict(counts))
"

If any entry has file not starting with "dataset/proprio/": the GT keys are wrong and
evaluate.py will silently produce NaN metrics. Fix by ensuring the script runs from project root.
  </action>

  <acceptance_criteria>
    - dataset/proprio/gt.json exists
    - python -c "import json; d=json.load(open('dataset/proprio/gt.json')); assert d['dataset']=='proprio'; assert len(d['images'])>=1; print('OK')" exits 0
    - All "file" keys in gt.json start with "dataset/proprio/" (no leading slash or "./")
    - All "condition" values are one of: bright, dark, lateral, overexposed
    - Each entry has at least one face with x1, y1, x2, y2 keys
    - Count of entries >= number of images captured (at minimum 1 per condition)
  </acceptance_criteria>

  <verify>
    <automated>python -c "
import json
from collections import Counter
d = json.load(open('dataset/proprio/gt.json'))
assert d['dataset'] == 'proprio', f'Wrong dataset: {d[\"dataset\"]}'
assert d['iou_threshold'] == 0.5
assert len(d['images']) >= 1, 'No entries in gt.json'
valid_conds = {'bright', 'dark', 'lateral', 'overexposed'}
for entry in d['images']:
    assert not entry['file'].startswith('/'), f'Absolute path: {entry[\"file\"]}'
    assert not entry['file'].startswith('./'), f'./ prefix: {entry[\"file\"]}'
    assert entry['file'].startswith('dataset/proprio/'), f'Wrong prefix: {entry[\"file\"]}'
    assert entry['condition'] in valid_conds, f'Invalid condition: {entry[\"condition\"]}'
    assert len(entry['faces']) >= 1, f'No faces in entry: {entry[\"file\"]}'
    f = entry['faces'][0]
    assert all(k in f for k in ('x1','y1','x2','y2')), f'Missing bbox keys in {entry[\"file\"]}'
counts = Counter(e['condition'] for e in d['images'])
print(f'gt.json valid: {len(d[\"images\"])} images, conditions: {dict(counts)}')
"</automated>
  </verify>

  <done>
    - dataset/proprio/gt.json exists with at least 1 entry per captured condition
    - All "file" keys are PROJECT_ROOT-relative strings starting with "dataset/proprio/"
    - All "condition" values match the folder names: bright, dark, lateral, overexposed
    - Script printed "GT written: ... (N images)" with N >= total images captured
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| labelme JSON files | Written by labelme GUI; structure is developer-controlled |
| dataset/proprio/ images | Captured by team members; stored locally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02-04 | Tampering | labelme JSON malformed | mitigate | parse_labelme_json returns None on missing shapes; WARNING printed; entry skipped |
| T-02-05 | Info Disclosure | images of team members on disk | accept | Academic project; images stored locally; not transmitted anywhere |
| T-02-06 | DoS | no images captured before conversion | mitigate | Script exits with warning if no entries found (sys.exit(1)) |
</threat_model>

<verification>
From project root, after Task 3 completes:

  python -c "
import json
from collections import Counter
d = json.load(open('dataset/proprio/gt.json'))
assert d['dataset'] == 'proprio'
assert d['iou_threshold'] == 0.5
counts = Counter(e['condition'] for e in d['images'])
print(f'Entries: {len(d[\"images\"])}, Conditions: {dict(counts)}')
# Check one sample key
sample = d['images'][0]['file']
print(f'Sample key: {sample!r}')
assert sample.startswith('dataset/proprio/'), 'GT key must start with dataset/proprio/'
print('gt.json validation passed')
"
</verification>

<success_criteria>
  - scripts/convert_labelme.py exists with correct syntax; all static checks pass
  - dataset/proprio/{bright,dark,lateral,overexposed}/ directories exist
  - After human annotation: dataset/proprio/gt.json exists with >= 1 entry per condition
  - gt.json: dataset=proprio, iou_threshold=0.5, all file keys start with "dataset/proprio/"
  - No absolute paths or "./" prefixes in any gt.json "file" value
</success_criteria>

<output>
After completion, create .planning/phases/02-datasets-annotations/02-02-SUMMARY.md
</output>
